<div>
  <table>
      <tr>
        <td>Tên </td>
        <td>Email</td>
        <td></td>
      </tr>
      <% @admins.each do |admin| %>
        <tr>
          <td><%=link_to admin.name, admin_edit_path(admin.id)%></td>
          <td><%= admin.email%></td>
          <td>
            <%= link_to 'Xóa', admin_delete_path(admin.id), data: {confirm: "Bạn có chắc muốn xóa?"}, method: :delete %>
          </td>
        </tr>
      <% end%>
    </table>
</div>
